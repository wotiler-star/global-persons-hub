# —— 全局知名人物志 · Hostinger 单进程部署环境变量示例 ——
# 复制为 .env.local（本地）或在 Hostinger hPanel「Node.js 应用 → 环境变量」中设置。

# 前端浏览器调用基址：同源 /api（由 Next Route Handlers 提供，单进程、免 CORS）
NEXT_PUBLIC_API_BASE=/api

# 可选：自定义人物数据目录。默认读取 <web 应用根>/data/persons.json（即 process.cwd()/data）。
# 若把数据放到其它绝对路径，可在此指定：
# GPH_DATA_DIR=/home/u123/public_html/apps/web/data

# 以下是可选增强（不配置也能跑；配置后语义检索/问答质量更高）：
# 语义向量维度（哈希兜底默认 384，与未配置嵌入模型时一致）
# GPH_EMBED_DIM=384
# 若接入 OpenAI 兼容嵌入服务（生产级语义），可在此指定：
# GPH_EMBED_API_URL=https://your-embed-endpoint/v1
# GPH_EMBED_API_KEY=sk-xxx
# GPH_EMBED_MODEL=text-embedding-3-small
